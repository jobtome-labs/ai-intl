{
  "name": "ai-intl",
  "version": "0.0.6",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/jobtome-labs/ai-intl"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "publish-packages": "turbo run build && changeset version && changeset publish"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@changesets/cli": "^2.25.2"
  }
}
